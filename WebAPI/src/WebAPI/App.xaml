<Application x:Class="WebAPI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- 深色主题资源 -->
        <SolidColorBrush x:Key="BgDark" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BgPanel" Color="#252526"/>
        <SolidColorBrush x:Key="BgInput" Color="#2D2D30"/>
        <SolidColorBrush x:Key="BgLog" Color="#282828"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#DCDCDC"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#858585"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#4A90D9"/>
        <SolidColorBrush x:Key="AccentRed" Color="#C83C3C"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#4EC9B0"/>
        <SolidColorBrush x:Key="BorderGray" Color="#3E3E42"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource BgDark}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource BgDark}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                    <Setter Property="Background" Value="{StaticResource BgDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
