# WebAPI - 网页端AI工具转本地API接口

> 将网页端官方在线AI工具转化为本地应用可用的 OpenAI-Compatible API 接口，免去 token 不足的焦虑。

**开源协议**: [MIT License](./LICENSE)

---

## 一、项目概述

### 1.1 背景与目标

当前主流 AI 厂商（DeepSeek、阿里千问、字节豆包等）都提供了网页端在线工具：

- **优点**：无需 API Key、额度充足、免登录可用
- **缺点**：仅限网页使用，无法被本地应用调用

本项目开发一套 **统一代理服务**，将网页端 AI 工具封装为标准 OpenAI-Compatible API 接口，使本地应用可以像调用普通 API 一样使用这些服务。

### 1.2 核心原理

```
┌─────────────┐         ┌──────────────────┐         ┌─────────────────┐
│ 本地应用     │  HTTP   │  本地代理服务      │ WebView2 │  官方网页端AI    │
│ (发送请求)   │ ──────> │  (端口监听+转发)  │ ───────> │  (实际执行)     │
│             │ <────── │  (转换协议)       │ <─────── │  (返回SSE流)    │
└─────────────┘         └──────────────────┘         └─────────────────┘
```

1. 本地应用向代理服务发送标准 OpenAI API 格式请求
2. 代理服务解析请求，通过 WebView2 自动填充网页输入框并提交
3. 代理服务拦截网页的 SSE 网络流，转换为 OpenAI-Compatible 格式响应
4. 返回给调用方（支持流式和非流式）

### 1.3 现有方案分析

| 特性    | DeepSeek          | Qwen千问             | 豆包         |
| ----- | ----------------- | ------------------ | ---------- |
| 端口    | 55555             | 56666              | 55556      |
| 目标URL | chat.deepseek.com | qianwen.aliyun.com | doubao.com |
| 拦截方式  | Fetch + XHR 拦截    | 同左                 | 同左         |
| SSE格式 | event: data: 解析   | 同左                 | 同左         |
| 注入方式  | DOM操作 + 事件模拟      | 同左                 | 同左         |

---

## 二、目录结构

```
D:\GitHub\WebAPI\
│
├── src/                          # 源代码
│   ├── WebAPI.sln               # 解决方案文件
│   ├── WebAPI/                  # 主程序 (WPF)
│   │   ├── App.xaml / App.cs    # 应用入口
│   │   ├── MainWindow.xaml      # 主窗口 (多标签页UI)
│   │   ├── MainWindow.cs        # 主窗口逻辑
│   │   ├── HttpServer.cs        # HTTP服务器
│   │   ├── RequestParser.cs     # 请求解析
│   │   ├── ResponseConverter.cs # 响应转换
│   │   └── Config.cs            # 配置管理
│   │
│   ├── Adapters/                # 各平台适配器
│   │   ├── IAdapter.cs          # 适配器接口定义
│   │   ├── DeepSeekAdapter.cs   # DeepSeek适配器
│   │   ├── QwenAdapter.cs       # 通义千问适配器
│   │   └── DoubaoAdapter.cs     # 豆包适配器
│   │
│   └── Common/                  # 公共组件
│       ├── SseParser.cs         # SSE流解析器
│       ├── JsonExtractor.cs     # JSON内容提取
│       └── LogManager.cs        # 日志管理
│
├── log/                         # 运行日志存放目录 ⭐
│   ├── 2026-05-27.log           # 按日期命名的日志文件
│   ├── 2026-05-28.log
│   └── ...
│
├── doc/                         # 更新说明文档保存目录 ⭐
│   ├── v1.0.0.md                # 版本更新说明
│   ├── v1.1.0.md
│   └── CHANGELOG.md             # 更新日志汇总
│
├── config/                      # 配置文件
│   └── settings.json            # 全局配置
│
├── LICENSE                       # MIT 开源协议
├── README.md                     # 项目说明文档
└── 开发文档.md                   # 本文件
```

### 目录说明

| 目录        | 用途                                            |
| --------- | --------------------------------------------- |
| `src/`    | 所有源代码，包含主程序、适配器、公共组件                          |
| `log/`    | **运行日志**，按日期自动生成，用于错误排查和问题定位。每次启动追加写入，保留最近30天 |
| `doc/`    | **更新说明文档**，记录每个版本的变更内容、新增功能、修复的Bug、已知问题等      |
| `config/` | 用户可编辑的配置文件                                    |

---

## 三、UI 设计 - 多标签页界面

### 3.1 整体布局

主窗口采用 **Tab 标签页切换** 方式，每个标签页对应一个 AI 渠道（DeepSeek / 千问 / 豆包）。参考现有 DeepSeek Relay 的布局设计：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [DeepSeek] [通义千问] [豆包] [+添加渠道]                          [_][□][X] │
├──────────────────────────────────────────────────────┬──────────────────────┤
│                                                      │  DeepSeek Relay      │
│                                                      │  ✅ 正在运行 (V46)   │
│              ┌──────────────────────┐                │                      │
│              │                      │                │  Base URL:           │
│              │   WebView2 嵌入区域   │                │  http://127.0.0.1:55555│
│              │   (加载目标网页)       │                │  [复制]               │
│              │                      │                │                      │
│              │                      │                │  API Key:            │
│              │                      │                │  sk-any              │
│              │                      │                │  [复制]               │
│              └──────────────────────┘                │                      │
│                                                      │  📋 可用模型组合:     │
│  ┌─────────────────────────────────────┐            │  标准对话: deepseek-chat│
│  │  给 DeepSeek 发送消息                 │            │  [复制]               │
│  │                                     │            │  联网对话: ...         │
│  │                                     │            │  [复制]               │
│  └─────────────────────────────────────┘            │  深度思考(R1): ...    │
│   [🔄深度思考] [🔍智能搜索]        [📎] [⬆️发送]     │  [复制]               │
│                                                      │  R1联网(weak): ...    │
│                                                      │  [复制]               │
│                                                      │                      │
│                                                      │  ┌─ 实时日志 ────────┐ │
│                                                      │  │[16:40:43] HTTP... │ │
│                                                      │  │[16:40:43] 正在初始化│ │
│                                                      │  │[16:40:43] WebView2 │ │
│                                                      │  │[16:40:43] Intercep│ │
│                                                      │  │[16:40:41] 页面就绪 │ │
│                                                      │  └───────────────────┘ │
│                                                      │                      │
│                                                      │  [🔄 重启浏览器内核]   │
│                                                      │  [⏹ 停止 HTTP 服务]   │
├──────────────────────────────────────────────────────┴──────────────────────┤
│  状态栏: DeepSeek服务就绪 | 端口:55555 | 请求计数:0                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 左侧区域 - WebView2 浏览器面板

- 占据窗口左侧约 **65%** 宽度
- 嵌入 WebView2 控件，加载目标 AI 平台网页
- 用户可直接在嵌入浏览器中操作（登录、手动聊天等）
- 支持以下操作按钮：
  - **深度思考** 切换按钮
  - **智能搜索** 切换按钮
  - **附件上传** 按钮
  - **发送** 按钮

### 3.3 右侧区域 - 控制面板

右侧控制面板从上到下分为以下区块：

#### ① 渠道状态区

```
📘 DeepSeek Relay
✅ 正在运行 (V46)    ← 绿色表示运行中，红色表示已停止
```

#### ② API 信息区

| 字段               | 说明                                  | 操作   |
| ---------------- | ----------------------------------- | ---- |
| Base URL (API地址) | 显示当前监听地址，如 `http://127.0.0.1:55555` | 复制按钮 |
| API Key (任意填写)   | 显示占位符 `sk-any`，用户可自定义               | 复制按钮 |

#### ③ 可用模型组合区

标题：**📋 可用模型组合 (点击框体复制):**

每个模型一行，显示模型名称 + 复制按钮：

**DeepSeek 渠道（端口 55555）** ✅ 已实测验证

| 模型类型     | 模型ID                         | 说明    | 测试状态 |
| -------- | ---------------------------- | ----- | ---- |
| 标准对话(极速) | `deepseek-chat`              | 默认模型  | ✅ 可用 |
| 联网对话(搜索) | `deepseek-chat-search`       | 带网络搜索 | ✅ 可用 |
| 深度思考(R1) | `deepseek-reasoner`          | 推理模式  | ✅ 可用 |
| R1联网(弱)  | `deepseek-reasoner-search`   | 推理+搜索 | ✅ 可用 |

> DeepSeek 官方 API 已推出 `deepseek-v4-flash`、`deepseek-v4-pro` 新模型，后续 `deepseek-chat`、`deepseek-reasoner` 可能弃用，届时需更新适配器。

**通义千问渠道（端口 56666）**

| 模型类型  | 模型ID          | 说明   |
| ----- | ------------- | ---- |
| 千问加速  | `qwen-turbo`  | 快速响应 |
| 千问增强  | `qwen-plus`   | 增强能力 |
| 千问旗舰  | `qwen-max`    | 最强模型 |
| 千问长文本 | `qwen-max-long` | 长文本支持 |

> 通义千问官方最新模型为 `qwen3.7-max`、`qwen3.6-plus`、`qwen3.6-flash`，当前网页端适配器使用 `qwen-turbo/plus/max/max-long` 映射，后续可扩展新模型。

**豆包渠道（端口 55556）**

| 模型类型 | 模型ID         | 说明    |
| ---- | ------------ | ----- |
| 豆包Pro | `doubao-pro`  | 高质量对话 |
| 豆包Lite | `doubao-lite` | 快速响应  |
| 角色扮演  | `doubao-role` | 角色对话  |

> 豆包对应火山方舟平台，模型列表持续更新中。

#### ④ 实时日志区

- 只读文本框，Consolas 等宽字体
- 深色背景 (`#282828`)，浅灰文字 (`#DCDCDC`)
- 自动滚动到最新日志
- 日志格式：`[HH:mm:ss] 日志内容`

#### ⑤ 操作按钮区

| 按钮         | 功能                         | 样式           |
| ---------- | -------------------------- | ------------ |
| 重启浏览器内核    | 重新初始化 WebView2（清缓存、重新加载页面） | 蓝色 `#4A90D9` |
| 停止 HTTP 服务 | 停止当前渠道的 HTTP 监听            | 红色 `#C83C3C` |

### 3.4 标签页行为

- 点击不同标签切换渠道，每个标签保持独立的：
  - WebView2 实例（不销毁，仅隐藏/显示）
  - HTTP 服务端口
  - 运行状态和日志
- 标签页显示渠道图标 + 名称 + 状态指示灯（绿=运行中/灰=未启动/红=错误）
- 支持 `+` 按钮动态添加新渠道（未来扩展）
- 右键标签可关闭/重启对应渠道

### 3.5 底部状态栏

```
DeepSeek服务就绪 | 端口:55555 | 请求计数:12 | 运行时间:01:23:45
```

---

## 四、技术架构

### 4.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                       WebAPI 主程序 (WPF)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    TabControl (多标签页)                    │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │  │
│  │  │ DeepSeek Tab │  │ 千问 Tab     │  │ 豆包 Tab     │  ...  │  │
│  │  │ ┌─────────┐ │  │ ┌─────────┐ │  │ ┌─────────┐ │       │  │
│  │  │ │WebView2  │ │  │ │WebView2  │ │  │ │WebView2  │ │       │  │
│  │  │ │+控制面板  │ │  │ │+控制面板  │ │  │ │+控制面板  │ │       │  │
│  │  │ └─────────┘ │  │ └─────────┘ │  │ └─────────┘ │       │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘       │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────┐  ┌───────────┐  ┌────────────┐  ┌─────────────┐  │
│  │HttpServer│  │Request   │  │Response   │  │Config       │  │
│  │(每标签独立)│  │Parser    │  │Converter  │  │Manager      │  │
│  └──────────┘  └───────────┘  └────────────┘  └─────────────┘  │
│                                                                 │
│  ┌──────────┐  ┌───────────┐  ┌──────────────────────────────┐  │
│  │SseParser│  │LogManager │  │ Adapter Factory              │  │
│  └──────────┘  └───────────┘  │ ├ DeepSeekAdapter           │  │
│                              │ ├ QwenAdapter               │  │
│                              │ └ DoubaoAdapter             │  │
│                              └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈

| 组件     | 技术选型                    | 说明             |
| ------ | ----------------------- | -------------- |
| UI框架   | WPF (.NET 8)            | Windows桌面应用    |
| 浏览器引擎  | Microsoft.WebView2      | 嵌入目标网页         |
| HTTP服务 | System.Net.HttpListener | 轻量级HTTP监听      |
| JSON处理 | System.Text.Json        | .NET原生JSON库    |
| 日志     | 自定义LogManager + 文件输出    | 输出到 `log/` 目录  |
| 开源协议   | MIT License             | 见 `LICENSE` 文件 |

---

## 五、API 接口规范

### 5.1 端点

```
POST http://127.0.0.1:{port}/v1/chat/completions    # 对话补全
GET  http://127.0.0.1:{port}/v1/models               # 模型列表
GET  http://127.0.0.1:{port}/health                  # 健康检查
POST http://127.0.0.1:{port}/shutdown                # 关闭服务
```

### 5.2 请求格式 (OpenAI Compatible)

```json
{
  "model": "deepseek-chat",
  "messages": [
    {"role": "system", "content": "你是..."},
    {"role": "user", "content": "用户问题"}
  ],
  "stream": true,
  "temperature": 0.7,
  "max_tokens": 2048
}
```

### 5.3 流式响应 (SSE)

```
Content-Type: text/event-stream

data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","created":xxx,"model":"deepseek-chat","choices":[{"index":0,"delta":{"content":"今天"},"finish_reason":null}]}

data: [DONE]
```

### 5.4 非流式响应 (JSON)

```json
{
  "id": "chatcmpl-xxx",
  "object": "chat.completion",
  "created": 1234567890,
  "model": "deepseek-chat",
  "choices": [{
    "index": 0,
    "message": { "role": "assistant", "content": "这是AI的回答..." },
    "finish_reason": "stop"
  }]
}
```

---

## 六、核心模块设计

### 6.1 主窗口 (MainWindow)

```csharp
// MainWindow.xaml 核心结构
<Window>
    <DockPanel>
        <!-- 顶部: 标签页栏 -->
        <TabControl x:Name="ChannelTabs">
            <!-- 每个 TabItem 包含一个 ChannelPanel -->
        </TabControl>

        <!-- 底部: 状态栏 -->
        <StatusBar x:Name="StatusBar"/>
    </DockPanel>
</Window>

// 每个 TabItem 的内容 = ChannelPanel (UserControl)
<UserControl x:Class="WebAPI.Controls.ChannelPanel">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />   <!-- 左: WebView2 -->
            <ColumnDefinition Width="320" />  <!-- 右: 控制面板 -->
        </Grid.ColumnDefinitions>

        <!-- 左侧: WebView2 + 输入区 -->
        <Grid Grid.Column="0">
            <WebView2 x:Name="WebView" />
            <!-- 底部输入栏叠加层 -->
            <DockPanel VerticalAlignment="Bottom">
                <TextBox x:Name="InputBox" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="深度思考" />
                    <Button Content="智能搜索" />
                    <Button Content="发送" />
                </StackPanel>
            </DockPanel>
        </Grid>

        <!-- 右侧: 控制面板 -->
        <StackPanel Grid.Column="1">
            <!-- 状态区 → API信息区 → 模型列表 → 日志区 → 操作按钮 -->
        </StackPanel>
    </Grid>
</UserControl>
```

### 6.2 HTTP 服务器模块

```csharp
public class ChannelHttpServer : IDisposable
{
    private HttpListener _listener;
    private readonly int _port;
    private readonly IAdapter _adapter;
    private readonly Action<string> _logCallback;

    public event EventHandler<ChatRequest>? RequestReceived;

    /// <summary>启动HTTP监听</summary>
    public async Task StartAsync(int port, IAdapter adapter, Action<string> logCallback);

    /// <summary>停止监听</summary>
    public void Stop();

    /// <summary>处理请求路由</summary>
    private async Task HandleRequest(HttpListenerContext ctx);
}
```

### 6.3 适配器接口

```csharp
public interface IAdapter
{
    string TargetUrl { get; }              // 目标平台URL
    string PlatformId { get; }             // 平台标识 (deepseek/qwen/doubao)
    int Port { get; }                      // 监听端口
    string UserDataFolder { get; }         // WebView数据目录
    List<ModelInfo> AvailableModels { get; } // 可用模型列表

    // DOM注入脚本
    string GetNetworkInterceptorScript();  // Fetch/XHR拦截脚本
    string GetDomControlScript();          // 输入框操作脚本
    Task InjectPromptAsync(WebView2 webView, string prompt);

    // SSE解析
    SseEvent? ParseSseData(string rawLine, ref string lastEventType);
}

public class ModelInfo
{
    public string Id { get; set; }          // 模型ID (如 deepseek-chat)
    public string Name { get; set; }        // 显示名称 (如 标准对话(极速))
    public string Description { get; set; } // 描述
}
```

### 6.4 SSE 解析器

```csharp
public class SseParser
{
    private StringBuilder _buffer = new();
    private string _lastEventType = "";

    /// <summary>解析原始SSE数据块，返回有效事件或null</summary>
    public SseEvent? Parse(string rawChunk)
    {
        _buffer.Append(rawChunk);
        // ... 按换行分割，逐行解析 event:/data:
        // 过滤内部状态消息 (title/update_session/search_result/status)
        // 提取 content 字段值
    }

    private bool IsInternalMessage(string json)
    {
        return json.Contains("\"p\": \"status\"")
            || json.Contains("\"accumulated_token_usage\"")
            || json == "FINISHED";
    }
}
```

### 6.5 日志管理

```csharp
public static class LogManager
{
    private static readonly string LogDir = Path.Combine(
        AppDomain.CurrentDomain.BaseDirectory, "log");

    private static string CurrentLogFile => Path.Combine(LogDir,
        $"{DateTime.Now:yyyy-MM-dd}.log");

    /// <summary>写日志 (同时输出到文件和控制台)</summary>
    public static void Write(string message, LogLevel level = LogLevel.Info)
    {
        string line = $"[{DateTime.Now:HH:mm:ss}] [{level}] {message}";
        Console.WriteLine(line);           // 控制台输出
        File.AppendAllText(CurrentLogFile, line + Environment.NewLine); // 文件输出
    }

    /// <summary>清理超过30天的旧日志</summary>
    public static void CleanupOldLogs(int keepDays = 30);
}
```

---

## 七、各渠道配置

### 7.1 settings.json

```json
{
  "server": {
    "host": "127.0.0.1"
  },
  "channels": [
    {
      "id": "deepseek",
      "name": "DeepSeek",
      "enabled": true,
      "port": 55555,
      "targetUrl": "https://chat.deepseek.com/",
      "icon": "💬",
      "models": [
        { "id": "deepseek-chat", "name": "标准对话(极速)" },
        { "id": "deepseek-chat-search", "name": "联网对话(搜索)" },
        { "id": "deepseek-reasoner", "name": "深度思考(R1)" },
        { "id": "deepseek-reasoner-search", "name": "R1联网(弱)" }
      ]
    },
    {
      "id": "qwen",
      "name": "通义千问",
      "enabled": true,
      "port": 56666,
      "targetUrl": "https://qianwen.aliyun.com/",
      "icon": "🔮",
      "models": [
        { "id": "qwen-turbo", "name": "千问加速" },
        { "id": "qwen-plus", "name": "千问增强" },
        { "id": "qwen-max", "name": "千问旗舰" }
      ]
    },
    {
      "id": "doubao",
      "name": "豆包",
      "enabled": true,
      "port": 55556,
      "targetUrl": "https://www.doubao.com/",
      "icon": "🫘",
      "models": [
        { "id": "doubao-pro", "name": "豆包Pro" },
        { "id": "doubao-lite", "name": "豆包Lite" }
      ]
    }
  ],
  "logging": {
    "level": "info",
    "directory": "log",
    "keepDays": 30
  }
}
```

---

## 八、开发计划

### Phase 1: 基础框架 (2-3天)

- [ ] 创建 WPF 项目结构 (.NET 9)
- [ ] 实现多标签页主窗口 UI（参考截图布局）
- [ ] 实现 HTTP 服务器基础框架
- [ ] 定义适配器接口 `IAdapter`
- [ ] 实现 SSE 解析器
- [ ] 实现 `log/` 日志系统
- [ ] 创建 `LICENSE` (MIT) 和 `README.md`

### Phase 2: DeepSeek 渠道 (2-3天)

- [ ] 分析 DeepSeek 最新页面 DOM 结构
- [ ] 实现 `DeepSeekAdapter`（拦截脚本 + 注入脚本）
- [ ] 集成到标签页，测试完整对话流程
- [ ] 测试流式/非流式响应
- [ ] 验证模型切换功能

### Phase 3: 通义千问渠道 (2天)

- [ ] 抓取千问页面 DOM 结构
- [ ] 实现 `QwenAdapter`
- [ ] 集成测试验证

### Phase 4: 豆包渠道 (2天)

- [ ] 抓取豆包页面 DOM 结构
- [ ] 实现 `DoubaoAdapter`
- [ ] 集成测试验证

### Phase 5: 完善与发布 (2天)

- [ ] 配置持久化（settings.json 读写）
- [ ] 渠道热启停（不重启程序）
- [ ] 编写 `doc/v1.0.0.md` 更新说明
- [ ] 编写完整 README
- [ ] 打包发布

---

## 九、使用示例

### 9.1 curl 测试

```bash
# DeepSeek 渠道
curl -X POST http://127.0.0.1:55555/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"deepseek-chat","messages":[{"role":"user","content":"你好"}],"stream":true}'

# 通义千问渠道
curl -X POST http://127.0.0.1:56666/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"qwen-turbo","messages":[{"role":"user","content":"你好"}],"stream":true}'
```

### 9.2 Python openai SDK

```python
from openai import OpenAI

# 连接 DeepSeek 渠道
client = OpenAI(api_key="sk-any", base_url="http://127.0.0.1:55555/v1")

resp = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": "解释量子计算"}],
    stream=True
)
for chunk in resp:
    if chunk.choices[0].delta.content:
        print(chunk.choices[0].delta.content, end="")
```

---

## 十、注意事项

### 10.1 开源协议 (MIT)

本项目采用 **MIT License** 开源协议：

- ✅ 允许商业使用
- ✅ 允许修改和分发
- ✅ 允许私有使用
- ⚠️ 需保留版权声明和许可声明
- ❌ 不提供任何担保

详见项目根目录 `LICENSE` 文件。

### 10.2 法律合规

- 仅封装网页已有功能，不逆向工程或破解任何服务
- 使用时请遵守各平台的服务条款
- 建议用于个人学习或开发测试目的

### 10.3 技术限制

- 需要保持 WebView2 内的登录状态（如平台要求登录）
- 依赖 WebView2 Runtime（Windows 10+ 自带）
- 目标网页 DOM 结构变更可能导致适配器失效，需及时更新适配器脚本
- 单次请求有额外延迟（HTTP转发 + WebView注入），不适合对延迟极度敏感的场景

### 10.4 日志说明

- 所有运行日志存放在 `log/` 目录下，按日期命名
- 日志级别：DEBUG / INFO / WARN / ERROR
- 自动清理超过 30 天的旧日志
- 出现问题时请提供对应日期的日志文件以便排查

---

## 十一、参考资料

### 现有方案来源

- `DeepSeek网页转API_20260527_154447.qka` — Quicker 动作（含完整 C# WebView2 + HttpListener 实现）
- `Qwen千问无限API_20260527_160135.qka` — Quicker 动作
- `豆包网页转API_20260527_154750.qka` — Quicker 动作

### 关键依赖

- **.NET 8 SDK** — [下载地址](https://dotnet.microsoft.com/download)
- **Microsoft.WebView2** — NuGet: `Microsoft.Web.WebView2`
- **WPF** — .NET 8 内置支持

### 外部资源

- [WebView2 官方文档](https://docs.microsoft.com/en-us/microsoft-edge/webview2/)
- [OpenAI API 规范](https://platform.openai.com/docs/api-reference/chat)
- [Quicker 软件](https://getquicker.net/) — 现有方案基于此自动化工具

---

> 文档版本: 1.2.0
> 更新日期: 2026-05-29
> 协议: MIT License
> 基于: DeepSeek/Qwen/豆包 Quicker动作文件分析 + UI截图参考设计 + 实测验证
