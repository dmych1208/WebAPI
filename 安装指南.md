# .NET 8 SDK 安装指南

## 为什么需要安装 .NET 8 SDK？

WebAPI 项目使用 .NET 8 开发，需要安装 .NET 8 SDK 才能编译和运行。

---

## 安装步骤

### 方法一：从微软官网下载（推荐）

1. **访问下载页面**
   - 打开浏览器访问：https://dotnet.microsoft.com/download/dotnet/8.0

2. **下载 SDK**
   - 选择 **.NET SDK 8.0.x**（Windows 版本）
   - 下载 **x64** 或 **x86** 版本（根据您的系统架构）

3. **安装**
   - 运行下载的安装程序
   - 按照提示完成安装
   - 无需特殊配置

4. **验证安装**
   打开 PowerShell 或命令提示符，运行：
   ```bash
   dotnet --version
   ```
   应该显示类似：`8.0.x`

### 方法二：使用 winget（命令行）

如果您使用 Windows 10/11，可以打开 PowerShell 运行：

```powershell
winget install Microsoft.DotNet.SDK.8
```

### 方法三：使用 Chocolatey

```powershell
choco install dotnet-sdk
```

---

## 安装后操作

### 1. 验证安装

```bash
# 检查版本
dotnet --version

# 检查已安装的 SDK
dotnet --list-sdks
```

### 2. 编译项目

```bash
# 进入项目目录
cd d:\GitHub\WebAPI

# 编译
dotnet build WebAPI.sln
```

### 3. 运行项目

```bash
# 运行主程序
dotnet run --project src\WebAPI\WebAPI.csproj
```

---

## 常见问题

### Q1: 安装后还是提示找不到 dotnet？

**解决方案：**
1. 重启终端或电脑
2. 检查 PATH 环境变量是否包含 .NET 路径

### Q2: WebView2 Runtime 是什么？

**说明：**
- WebView2 是微软的浏览器引擎，Windows 10/11 通常自带
- 如果启动时提示未安装，从这里下载：
  https://developer.microsoft.com/en-us/microsoft-edge/webview2/

### Q3: 需要管理员权限吗？

**说明：**
- .NET SDK 安装通常需要管理员权限
- 但运行编译后的程序不需要

---

## 快速验证清单

安装完成后，验证以下命令：

- [ ] `dotnet --version` 显示 8.0.x
- [ ] `dotnet --list-sdks` 显示至少一个 8.0 SDK
- [ ] `dotnet build WebAPI.sln` 编译成功
- [ ] 程序能够正常启动

---

## 其他资源

- **.NET 官方文档**：https://docs.microsoft.com/zh-cn/dotnet/
- **WebView2 下载**：https://developer.microsoft.com/en-us/microsoft-edge/webview2/
- **Visual Studio**：如果您使用 Visual Studio 2022，它内置了 .NET 8 支持

---

> 安装完成后，您就可以运行 WebAPI 项目了！
